/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.takapedia.com', 'ik.imagekit.io', 'lh3.googleusercontent.com', 'assets.lapakgaming.com']
    }
};

export default nextConfig;
